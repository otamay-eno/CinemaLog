<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CinemaStudy" parent="Theme.MaterialComponents.DayNight">
        <!-- アクションバーの設定 -->
        <item name="actionBarSize">40dip</item>
        <item name="android:gravity">center</item>
        <item name="android:actionBarStyle">@style/CustomActionBarNight</item>
        <item name="actionBarStyle">@style/CustomActionBarNight</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primarycolor</item>
        <item name="colorPrimaryVariant">@color/primarycolor</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primarycolor</item>
        <item name="colorSecondaryVariant">@color/primarycolor</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/nearlyblack</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- 背景色の設定とタイトルカラーのスタイル設定 -->
    <style name="CustomActionBarNight" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@drawable/actionbar_background_night</item>
        <item name="background">@drawable/actionbar_background_night</item>
        <item name="titleTextStyle">@style/titleTextStyleNight</item>
    </style>
    <!-- タイトルカラーの設定 -->
    <style name="titleTextStyleNight" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>